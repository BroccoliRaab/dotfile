##!/bin/sh

while read -r line; do
    case $line in
        ptoggle)
            mpc -q toggle
            ;;
        next)
            mpc -q next
            ;;
        prev)
            mpc -q prev
            ;;
    esac
done
